# The name this host takes on the tailnet. Pinned rather than left to default
# so the address stays predictable: `ssh daniel@xps` works from any device on
# the tailnet without looking up which 100.x address was handed out this time.
tailscale_hostname: xps
