- name: Restart journald
  # Applies a changed size cap without waiting for a reboot. Safe: journald
  # hands its sockets back to PID 1, so messages logged during it are buffered.
  ansible.builtin.systemd:
    name: systemd-journald
    state: restarted
