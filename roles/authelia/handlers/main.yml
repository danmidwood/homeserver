- name: Restart Authelia
  # Authelia reads its configuration only at startup. Sessions do not survive
  # this; registered passkeys do, because they live in the database.
  community.docker.docker_container:
    name: authelia
    state: started
    restart: true
